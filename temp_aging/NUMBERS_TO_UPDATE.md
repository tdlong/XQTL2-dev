# What changes when the rerun lands

The prose in `RESULTS.md` is Tony's current text, stored clean. This is
the checklist for updating it against the new `numbers/`. Nothing here is a
judgement about the writing — only about which figures are still valid after the
h2 estimator and covariance fixes (XQTL2 #40).

Source for every replacement is `numbers/`, regenerated by `reproduce.sh all`.

## Paragraph 1 — new numbers, same argument

| in the text | fate | from |
|---|---|---|
| 3–11% selected, 10 replicates | stands | design files |
| 268 tiles | stands — the tiling is off the genetic map | `scan_resolution` |
| 37% at 7.5, 12% at 15 | **changes** | `scan_resolution` |
| 8 and 15 peaks, hence 23 | **changes** | `peak_table` |
| 0.26–1.54 cM, 167–919 kb | **changes** | `peak_table` |

The covariance repair moves every Wald by up to 1.23 per window, almost always
upward, so expect slightly more significant tiles rather than fewer.

## Paragraph 2 — the floor argument needs rebuilding, not renumbering

"Heritability estimates are necessarily positive, so tiles whose Wald never
exceeds 2 define a heritability floor of 0.68%" was true of the old estimator,
and 0.68% *was* its squaring bias. The new `H2` can go negative; `H2_vc` is
non-negative only because zero is a boundary solution of the likelihood, not a
clamp. So the premise is gone and the sentence has to be rewritten from it.

| in the text | fate |
|---|---|
| floor 0.68% | **void** — it was the bias |
| 1.00% at 7.5, 1.31% at 15 | **changes**; h2 of 1% now sits near Wald 15–30, not 7.5 |
| "slightly more than 1%" | rests on the above, revisit |
| ~100,000 screened per treatment | stands |

The power claim — that a 1–2% locus would have gone undetected in several
hundred RILs — is now calculable rather than asserted. `H² ≈ 100k·T/(n_eff i²)`
(Methods) inverts to `n_eff = 100k·T/(H² i²)`, so naming a comparison design
gives the number directly. For this design the 7.5 threshold sits at 0.38%.
| "~25 thousand selected and control" | **wrong regardless**: 22,896 selected + 13,892 control = 36,788 |

## Paragraph 3 — rewrite, not renumber

The claim was the artefact. h2 squared within each replicate before averaging,
and the leading constant assumed two allele copies per fly, wrong for a
hemizygous male X. Corrected, male X h2 falls roughly fivefold and the X stops
standing out — so "male heritability exceeds female at every one of the 15
significant tiles on the X" has no basis, and the hemizygosity remark goes with
it.

Every number moves: 98 tiles, the 15 X tiles, 52% of 83, and the
15.3% / 1.6% / −0.4% / 12.8% partition. The partition additionally had the old
bias inside its shared component, so its fractions shift for reasons that are
not biology — do not read the change as a result.

What survives is the diet finding, which was never X-dependent.

## Paragraph 4 — mostly intact, because most of it is frequencies

| in the text | fate |
|---|---|
| B3 at 15.1%, falling 6.3–10.3 points | stands — frequencies, not heritabilities |
| B3 flat across the ten cages (Fig. 2h) | stands |
| −log10 P of 163.8; "exceeds 36" | **changes** — Wald |
| 55 kb / 0.22 cM interval | **changes** — read off the Wald |
| 4.5% and 2.2% of phenotypic variance | **void** — h2 |
| "low yeast females" | typo, should read low-sugar |
| PGRP-LA | never checked against an annotation |
