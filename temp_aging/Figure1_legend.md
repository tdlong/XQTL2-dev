Figure 1:  An XQTL of lifespan.  (A) Wald test of haplotype frequency change
between the selected long-lived pool and its unselected control. (B) Heritability,
h², estimated from haplotype frequency shifts expressed as a percentage of
phenotypic variance attributable to the window. (C) h² partitioned into the
component shared by all four treatments (main), the component that differs
between the sexes (sex), and the component that differs between diets (diet).

---

<!-- Everything below this line is Claude's, unreviewed. Tony's legend is the
     text above, verbatim. An earlier version of this file appended ~50 lines of
     numbers, a methods description and interpretation ("reflects hemizygosity
     ...") on top of it, including a figure derived from the genome-rescaled-to-50%
     calculation that was later withdrawn. Removed. -->

QUERY, not a proposed edit: the figure now comes from the 10-replicate dataset
(replicates 8 and 9 dropped). Nothing in the legend states a replicate count, so
nothing needs changing unless you want one stated.
